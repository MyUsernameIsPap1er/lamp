
RewriteEngine On
RewriteBase /

# Don't rewrite existing files or dirs
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send API requests to the backend router
RewriteRule ^api/(.*)$ api/index.php?route=$1 [QSA,L]